[project]
name = "qlora-mao"
version = "0.1.0"
description = "Scrape and process Mao Zedong's writings into QLoRA training data"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "beautifulsoup4",
    "lxml",
    "openai",
]
